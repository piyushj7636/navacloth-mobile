import { Stack } from "expo-router";

export default function RewardsLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: true,
				headerTitle: "Rewards"
      }}
    />
  );
}
